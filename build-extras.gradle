// Contents of platforms/android/app/build-extras.gradle

configurations.all {
    resolutionStrategy {
        // Attempt to force an older version of error_prone_annotations
        // instead of the problematic 2.36.0
        force 'com.google.errorprone:error_prone_annotations:2.26.1'
    }
}